
# File Renamer
	cinst ant-renamer -y --ignore-checksum

# Digital entertainment
	cinst calibre -y
	# cinst handbrake.install -y ## Broken package
	cinst mediainfo-cli -y
	cinst QuickPar -y --allow-empty-checksums
	cinst mkvtoolnix -y
	cinst youtube-dl -y

	
# Utilities

	# Apply tweaks - PATH variables
	if(!(Test-Path -Path "C:\tools\" )){
		New-Item -ItemType directory -Path "c:\tools\"
	}
	Invoke-WebRequest 'https://raw.githubusercontent.com/iRobie/New-Computer/master/src/tweaks/Personal-Media-Tweaks.ps1' -OutFile 'C:\tools\Personal-Media-Tweaks.ps1'
		# Remove illegal dashes
			(Get-Content C:\tools\Personal-Media-Tweaks.ps1 -Encoding UTF8).replace('[\u2012\u2013\u2014]', '-') | Set-Content C:\tools\Personal-Media-Tweaks.ps1

	&  'C:\tools\Personal-Media-Tweaks.ps1'
