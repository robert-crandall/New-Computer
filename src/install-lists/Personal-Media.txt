
# File Renamer
	cinst ant-renamer -y --ignore-checksum

# Digital entertainment
	cinst calibre -y
	# cinst handbrake.install -y ## Broken package
	cinst mediainfo-cli -y
	cinst QuickPar -y --allow-empty-checksums
	cinst mkvtoolnix -y
	cinst youtube-dl -y

	
## Add Handbrake to system path
$AddedLocation ="C:\Program Files\Handbrake"
$Reg = "Registry::HKLM\System\CurrentControlSet\Control\Session Manager\Environment"
$OldPath = (Get-ItemProperty -Path "$Reg" -Name PATH).Path
$NewPath= $OldPath + ';' + $AddedLocation
Set-ItemProperty -Path "$Reg" -Name PATH –Value $NewPath
